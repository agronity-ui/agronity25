/** Tailwind v3 PostCSS config.
 *  Do not upgrade tailwindcss to v4 unless this file is changed to @tailwindcss/postcss
 *  and globals.css is migrated to the v4 import style.
 */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}
  }
};

export default config;
